import{describe,expect,it}from"vitest";import{basisBps,changeBps,fixedHoldingReturn,floatingHoldingReturn,maxCapacityAtRate,percentile,rateChangeVolatilityBps}from"../src/index.js";
describe("financial semantics",()=>{it("basis",()=>expect(basisBps(.0684,.0569)).toBe(115));it("holding semantics",()=>{expect(fixedHoldingReturn(.06,365)).toBeCloseTo(.06);expect(floatingHoldingReturn(.06,90)).toBeCloseTo(.06*90/365)});it("percentile",()=>{expect(percentile(.05,[])).toBeNull();expect(percentile(2,[1,2,3,4])).toBe(50)});it("change",()=>expect(changeBps(.051,.05)).toBeCloseTo(10));it("rate-change vol",()=>expect(rateChangeVolatilityBps([.05,.051,.052,.049])).not.toBeNull());it("capacity interpolation",()=>{const x=maxCapacityAtRate([{notionalUsd:100000,rate:.06,fillable:true},{notionalUsd:500000,rate:.055,fillable:true},{notionalUsd:1000000,rate:.045,fillable:true}],.05,"at-least");expect(x?.estimated).toBe(true);expect(x?.notionalUsd).toBeGreaterThan(500000);expect(x?.notionalUsd).toBeLessThan(1000000);});});
