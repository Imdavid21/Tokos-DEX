import{test,expect}from"@playwright/test";test("shell and research routes render",async({page})=>{await page.goto("/");await expect(page.locator("header")).toContainText("tokos");await page.goto("/markets");await expect(page.getByRole("heading",{name:"Markets"})).toBeVisible();await page.goto("/rates?asset=usdc&notional=100000&horizon=90");await expect(page.getByRole("heading",{name:/USDC rates/i})).toBeVisible();await page.goto("/datasets");await expect(page.getByRole("heading",{name:"Datasets"})).toBeVisible()});
