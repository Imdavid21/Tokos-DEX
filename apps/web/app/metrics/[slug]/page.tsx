import {notFound} from "next/navigation";
import {maybeApi} from "@/lib/api";
import {Metric} from "@/components/Metric";
type Data={slug:string;name:string;unit:string;source:string;formula:string;frequency:string;methodologyVersion:string;availableHistory:{firstObservation:string|null;latestObservation:string|null;observations:number}};
export default async function MetricDetail({params}:{params:Promise<{slug:string}>}){const{slug}=await params,res=await maybeApi<Data>(`/metrics/${encodeURIComponent(slug)}`);if(!res)notFound();const d=res.data;return <div className="page"><div className="page-head"><h1 className="page-title">{d.name}</h1><div className="page-meta">{d.unit}</div></div><div className="metrics"><Metric label="Observations" value={d.availableHistory.observations.toLocaleString()}/><Metric label="First observation" value={dateOnly(d.availableHistory.firstObservation)}/><Metric label="Latest observation" value={dateOnly(d.availableHistory.latestObservation)}/><Metric label="Methodology" value={d.methodologyVersion}/></div><section className="section"><h2 className="section-title">Definition</h2><dl className="compact-provenance"><Row label="Source" value={d.source}/><Row label="Formula" value={d.formula}/><Row label="Frequency" value={d.frequency}/><Row label="Unit" value={d.unit}/></dl></section></div>}
function dateOnly(v:string|null){return v?new Date(v).toISOString().slice(0,10):"—"}function Row({label,value}:{label:string;value:string}){return <div><dt>{label}</dt><dd>{value}</dd></div>}
