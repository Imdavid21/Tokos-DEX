import {maybeApi,qs} from "@/lib/api";
import {CorrelationMatrix} from "@/components/IntelligenceVisuals";
import {Unavailable} from "@/components/DataState";
export const metadata={title:"Correlations"};
type Data={dimension:"market"|"protocol"|"asset";days:number;labels:Array<{key:string;label:string}>;pairs:Array<{xKey:string;yKey:string;xLabel:string;yLabel:string;coefficient:number|null;samples:number;sampleFrom:string|null;sampleTo:string|null}>};
export default async function Correlations({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const s=await searchParams,dimension=(one(s.dimension)==="protocol"||one(s.dimension)==="asset"?one(s.dimension):"market") as "market"|"protocol"|"asset",days=[30,90,180,365].includes(Number(one(s.days)))?Number(one(s.days)):90,res=await maybeApi<Data>(`/analytics/correlations?${qs({dimension,days,limit:12})}`);return <div className="page"><div className="page-head"><h1 className="page-title">Correlations</h1><div className="page-meta">Minimum 7 overlapping daily observations</div></div><form className="filterbar" method="get"><label>Dimension<select name="dimension" defaultValue={dimension}><option value="market">Markets</option><option value="protocol">Protocols</option><option value="asset">Assets</option></select></label><label>Window<select name="days" defaultValue={String(days)}><option value="30">30D</option><option value="90">90D</option><option value="180">180D</option><option value="365">1Y</option></select></label><button type="submit" className="btn">Apply</button></form><section className="panel section"><div className="panel-head"><span className="panel-title">Correlation Matrix</span><span className="panel-note">Normalized rates</span></div><div className="panel-body">{res&&res.data.labels.length>1?<CorrelationMatrix labels={res.data.labels} pairs={res.data.pairs}/>:<Unavailable title="Insufficient overlapping history" detail="Correlations are suppressed until enough real overlapping observations exist."/>}</div></section></div>}
function one(v:string|string[]|undefined){return Array.isArray(v)?v[0]:v}
