import{maybeApi}from"@/lib/api";import{pct}from"@/lib/format";export const metadata={title:"Risk / Yield Index"};type R={id:string;rate:number|null;rating:{rating:string;score:number|null;coverage:number}};export default async function Page(){const r=await maybeApi<R[]>("/risk/markets?limit=500"),rows=r?.data??[],grades=["AAA","AA","A","BBB","BB","B","CCC"];const bands=grades.map(g=>{const a=rows.filter(x=>x.rating?.rating===g&&x.rate!=null);return{g,n:a.length,y:a.length?a.reduce((s,x)=>s+(x.rate??0),0)/a.length:null}});return <div className="page"><div className="page-head"><div><h1 className="page-title">Risk / Yield Index</h1><div className="page-sub">A common axis for asking whether yield compensates for observable risk. Simple averages are shown until volume-weighted benchmark coverage is sufficient.</div></div></div><div className="rating-scale">{bands.map(x=><div key={x.g}><b>{x.g}</b><span>{x.n} markets</span><strong>{pct(x.y)}</strong></div>)}</div><section className="section"><div className="panel"><div className="panel-head"><span className="panel-title">How to read the index</span></div><div className="panel-body">Compare yield within the same evidence-rating band first. Higher yield at the same rating can indicate relative value, while a higher headline APY paired with a weaker rating may simply be compensation for observable risk.</div></div></section></div>}