import Link from"next/link";import{maybeApi}from"@/lib/api";import{Unavailable}from"@/components/DataState";
export const metadata={title:"Dependencies"};
type E={source_type:string;source_id:string;relationship:string;target_type:string;target_id:string;exposure_usd:number|null;weight:number|null;observed_at:string};
type R={root:{type:string;id:string};edges:E[]};
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const s=await searchParams,type=one(s.type)??"market",id=one(s.id)??"",r=id?await maybeApi<R>(`/dependencies/${type}/${encodeURIComponent(id)}/graph?depth=2`):null;return <div className="page"><div className="page-head"><div><h1 className="page-title">Dependency Map</h1><div className="page-sub">Observed relationships only. Missing dependencies remain missing.</div></div></div><form className="filter-bar"><select className="select" name="type" defaultValue={type}>{["market","asset","protocol","chain","vault","curator","oracle","issuer","bridge"].map(x=><option key={x}>{x}</option>)}</select><input className="input" name="id" defaultValue={id} placeholder="Entity ID"/><button className="btn">Map</button></form>{!id?<Unavailable title="Choose an entity" detail="Enter an entity ID to traverse its observed dependency graph."/>:!r?<Unavailable title="Dependency graph unavailable"/>:<div className="table-wrap"><table className="data"><thead><tr><th>Source</th><th>Relationship</th><th>Target</th><th>Exposure</th><th>Weight</th><th>Observed</th></tr></thead><tbody>{r.data.edges.map((e,i)=><tr key={i}><td className="mono">{e.source_type}:{e.source_id}</td><td>{e.relationship}</td><td className="mono">{e.target_type}:{e.target_id}</td><td>{e.exposure_usd??"—"}</td><td>{e.weight??"—"}</td><td>{new Date(e.observed_at).toISOString()}</td></tr>)}</tbody></table></div>}</div>}function one(v:string|string[]|undefined){return Array.isArray(v)?v[0]:v}
