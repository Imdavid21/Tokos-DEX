export function Metric({label,value,meta,className=""}:{label:string;value:string;meta?:string;className?:string}){return <div className={`metric ${className}`}><div className="metric-label">{label}</div><div className="metric-value">{value}</div>{meta&&<div className="metric-meta">{meta}</div>}</div>}
